version: 2.1

commands:
  install_deps:
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: npm install
  run_linter:
    steps:
      - checkout
      - run: npm run lint
  run_test:
    steps:
      - checkout
      - run: npm test -- --no-watch --no-progress --browsers=ChromeHeadlessCI
  build_electron_pkg:
    steps:
      - checkout
      - run:
          name: build electron package
          command: ./scripts/electron-build.sh

executors:
  my-executor:
    docker:
      - image: 'circleci/node:erbium'
    working_directory: /tmp

jobs:
  lint-and-test:
    docker:
      - image: circleci/node:erbium-browsers
    steps:
      - install_deps
      - run_linter
      - run_test
  build_packages:
    docker:
      - image: circleci/node:erbium
    steps:
      - install_deps
      - run_linter
      - build_electron_pkg
      - persist_to_workspace:
          root: workspace
          paths:
            - dist
  publish-github-release:
    docker:
      - image: cibuilds/github:0.10
    steps:
      - attach_workspace:
          at: /tmp/workspace
      - run:
          name: 'Publish Release on GitHub'
          command: |
            if [ "$(ls -A /tmp/workspace/dist/)" ]
            then
              ghr -t ${GITHUB_TOKEN} -u ${CIRCLE_PROJECT_USERNAME} -r ${CIRCLE_PROJECT_REPONAME} -c ${CIRCLE_SHA1} ${CIRCLE_TAG} /tmp/workspace/dist/
            else
              echo "No files found"
            fi

workflows:
  lint-and-test:
    jobs:
      - lint-and-test
  build-and-release:
    jobs:
      - build_packages
      - publish-github-release:
          requires:
            - build_packages
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^saza-v*/
